# Dépendances LOCALES de développement et de test.
#
# Le RUNTIME est hérité par référence de requirements.prod.txt (= la source de
# vérité, ce qui tourne réellement en prod et en CI). Le venv local reçoit donc
# EXACTEMENT les mêmes versions qu'en production — plus aucune dérive possible
# (redis, email-validator, apscheduler[redis]… sont inclus automatiquement).
#
# RÈGLE : n'ajouter ci-dessous QUE des outils de dev/test. Toute dépendance de
# runtime va dans requirements.prod.txt, jamais ici.
-r requirements.prod.txt

# Tests
# Versions alignees sur ce que la CI installe et fait passer au vert.
# ATTENTION : pytest-asyncio < 1.0 exige pytest<9 — le fichier declarait
# pytest==9.0.3 AVEC pytest-asyncio==0.23.7, combinaison impossible a installer.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
testcontainers[postgres]==4.14.2
aiosqlite==0.22.1

# Qualité & sécurité statique — versions alignées sur celles que la CI exécute
# (ruff/mypy/bandit/pip-audit tournent dans le job Backend). Les hooks pre-commit
# épinglent leurs propres versions dans .pre-commit-config.yaml.
ruff==0.15.22
mypy==2.3.0
sqlalchemy[mypy]==2.0.49
bandit==1.9.4
pip-audit==2.10.1
pre-commit==4.5.1

# Retires le 2026-07-17 : semgrep, detect-secrets et checkov etaient declares ici
# mais n'etaient JAMAIS installes (absents du venv) ni JAMAIS executes (aucun step
# CI, aucun hook pre-commit ; il ne reste que des commentaires `# checkov:skip`).
# En prime, semgrep et detect-secrets avaient des dependances incompatibles entre
# elles, ce qui rendait ce fichier impossible a installer depuis zero.
# Pour les reintroduire : les ajouter comme vrai step CI ou hook pre-commit, pinnes.
