"""Boite a outils des documents PDF — identite visuelle et mises en page.

Ce paquet ne re-exporte RIEN, volontairement : c'est un re-export de
compatibilite qui avait referme le cycle entre `brand` et `covers`, corrige le
2026-08-18. On importe le sous-module directement.

    from app.services.pdf import brand
"""
