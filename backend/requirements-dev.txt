# Dépendances de développement — NE PAS installer en production
# Usage local : pip install -r requirements-dev.txt
# CI : bandit et pip-audit sont installés explicitement dans ci.yml
-r requirements.prod.txt

# Tests
aiosqlite
pytest
pytest-asyncio
httpx
email-validator
pytest-cov
testcontainers[postgres]

# Analyse de sécurité statique
bandit==1.9.4
pip-audit==2.10.0
semgrep==1.72.0
detect-secrets==1.5.0
checkov==3.2.0
njsscan==0.3.7
