/**
 * Source unique de vérité pour la version de l'application (côté frontend).
 * Mis à jour automatiquement par scripts/bump-version.sh — ne pas modifier manuellement.
 */
export const APP_VERSION = '0.6.0';
