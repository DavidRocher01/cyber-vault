================================================================================
  CYBERSCAN — JOURNAL DE DÉVELOPPEMENT
  Traçabilité complète des fonctionnalités implantées jour par jour
================================================================================


────────────────────────────────────────────────────────────────────────────────
  2026-04-21
────────────────────────────────────────────────────────────────────────────────

  TESTS
  • Couverture tests portée à 88.93% (objectif 85% atteint — 699 TU)
    - test_ssrf_unit.py (15 tests) : _is_private(), assert_no_ssrf()
    - test_iso27001_unit.py (21 tests) : structure 38 items, score, endpoints
    - test_public_scan_unit.py (9 tests) : run_public_scan(), PublicScanOut
    - Correction test_users.py : champ notif_ssl_expiry manquant dans payloads
    - Correction test_url_scans.py : patch assert_no_ssrf pour DNS flaky

  FEATURES — Demo Scan Public (sans compte)
  • Nouveau modèle PublicScan (session_token UUID, sans FK user/site)
  • Service de scan rapide : 9 modules (SSL, headers, email, cookies, CORS,
    IP reputation, DNS, CMS, WAF) — sans ports, sans breach, sans PDF
  • Endpoints rate-limités à 3/heure par IP (protection abus)
  • Migration Alembic : table public_scans
  • Frontend : formulaire URL sur la landing page (section demo)
  • Page DemoResultComponent : polling toutes les 3s, score/jauge, 9 cartes
    modules colorées, CTA "Créer un compte" / "Voir les tarifs"
  • Route /cyberscan/demo-result/:token (sans auth guard)

  FEATURES — Dashboard amélioré
  • Badge SSL par site : jaune (≤30j), orange (≤14j), rouge (≤7j)
  • Bouton "DNS" par site → page sous-domaines
  • Tuile "Sous-domaines" dans la grille des outils rapides (dashboard)

  FEATURES — Scan de sous-domaines
  • Endpoint GET /api/v1/sites/{id}/subdomains (extrait DNS du dernier scan)
  • SubdomainsComponent : tableau filtrable (sous-domaine + IP), alerte zone
    transfer CRITICAL, état vide avec lien dashboard
  • Route /cyberscan/subdomains/:id (authGuard)

  TESTS — SSL Alerts (41 TU)
  • test_ssl_alerts_unit.py (16 tests) : logique déduplication, escalade
    seuils 30/14/7 jours, multi-sites, échec email silencieux
  • test_ssl_email_unit.py (15 tests) : HTML/texte, couleurs CRITIQUE/
    URGENT/ATTENTION, sujet [Rocher Cybersécurité] prefix
  • test_notif_prefs_unit.py (6 tests) : persistance notif_ssl_expiry, schémas

  FEATURES — Alertes SSL
  • Job APScheduler quotidien 09h00 UTC : _check_ssl_alerts
  • Envoi email à 30j / 14j / 7j avec escalade (jamais de downgrade)
  • Champs DB : ssl_alert_threshold, ssl_alert_sent_at (sites),
    notif_ssl_expiry (users)
  • Toggle "Alertes expiration SSL" dans la page profil/notifications

  SEO
  • Canonical URLs + meta descriptions corrigés sur toutes les pages publiques

  INFRA / CI
  • Désactivation Dependabot pour les packages npm frontend
  • Fix pin @ngneat/overview v5 (DynamicViewModule supprimé en v6)
  • Monitoring Sentry ajouté (frontend + backend)
  • Migration email SMTP → Resend API
  • Fix domaine Resend : utilisation shared domain pendant vérification
  • CloudFront : custom error pages pour le routing Angular SPA
  • Fix pip-audit 2.7.3 (conflit tomli/semgrep)
  • Inject FRONTEND_URL + ADMIN_API_KEY dans ECS task definition


────────────────────────────────────────────────────────────────────────────────
  2026-04-20
────────────────────────────────────────────────────────────────────────────────

  FIXES
  • Résolution des URLs relatives og:image (// et /path) dans la newsletter


────────────────────────────────────────────────────────────────────────────────
  2026-04-19
────────────────────────────────────────────────────────────────────────────────

  NEWSLETTER ADMIN
  • Page admin /cyberscan/admin/newsletter
  • Refonte newsletter : articles avec liens hypertexte au lieu d'éditorial
  • 6 slots d'articles pré-remplis toujours visibles
  • Ajout image_url optionnelle par article (auto-fetch og:image)
  • Fix : persistance image_url dans PUT /admin/schedule
  • Fix : revision Alembic image_url (down_revision incorrect corrigé)

  SÉCURITÉ
  • Upgrade hachage tokens : SHA-256 → HMAC-SHA256 + resize colonnes
  • Durcissement SSRF : protection DNS rebinding + chaîne de redirections
  • Rate limiter : extraction correcte IP client derrière ALB/CloudFront
  • Fix : PAT GitHub jamais stocké en base — credentials masqués dans logs
  • Fix : scan dans un thread dédié pour libérer l'event loop FastAPI

  UX / FEATURES
  • Flip cards : explication fonctionnelle au clic sur chaque module de scan
  • Tests unitaires : NewsletterConfirmComponent
  • Fix : newsletter confirmation — appel API backend avec token

  CI / INFRA
  • Fix docker build context + frontend legacy-peer-deps
  • Fix IAM PassRole dans le pipeline CI/CD
  • Fix : trigger deploy sur branche master (pas seulement main)


────────────────────────────────────────────────────────────────────────────────
  2026-04-17
────────────────────────────────────────────────────────────────────────────────

  SÉCURITÉ (Round 3)
  • Correction fuites PII, upgrades CVE, durcissement Dockerfile
  • Fix CVE python-jose : upgrade dépendance
  • Harden migrate.py + ajout detect-secrets baseline
  • Suppression false positives bandit (verify=False)
  • Upgrade Angular 17 → 19 (correctifs XSS/XSRF)

  REFACTORING
  • Extraction helpers partagés : paginate, crud, datetime_utils,
    assessment_service, subscription_service
  • Fix 6 bugs notables : datetime timezone, seed DB, create_all race,
    DEV_MODE, newsletter, conflit packaging safety/checkov

  TESTS
  • Fix chemin scanner dans test_remediation.py (parents[3] → parents[2])
  • Fix mocks lastScanStatus dans les specs du dashboard (overall_status)
  • Restauration standalone: true sur 34 composants après migration Angular 19


────────────────────────────────────────────────────────────────────────────────
  2026-04-16
────────────────────────────────────────────────────────────────────────────────

  FEATURES — Code Scan
  • 10 nouveaux outils d'analyse de code (18 outils au total)
  • Fix : prévenir crash lors du lancement d'un second scan concurrent
  • Fix : résolution conflit packaging safety/checkov/pip-audit

  UX — Dashboard & Landing
  • Rafraîchissement systématique du dashboard après déclenchement d'un scan
  • Fix scroll vers #pricing (4 itérations de correction scrollIntoView /
    getBoundingClientRect / anchorScrolling / overflow-x:clip)

  TESTS
  • Couverture _run_scan_sync, run_in_executor, guard 429 code-scans


────────────────────────────────────────────────────────────────────────────────
  2026-04-15
────────────────────────────────────────────────────────────────────────────────

  FEATURES — Remédiation
  • 10 nouveaux scripts de remédiation (+ tests complets)
  • Affichage de tous les scripts dans la page scan-detail
  • Fix : passage de tous les résultats de scan à generate_remediation
  • Fix : ajout des 12 nouveaux scripts dans _REMEDIATION_META
  • Fix : fallback régénération on-the-fly si fichiers manquants
  • Couverture complète des téléchargements de scripts de remédiation (tests)

  FEATURES — Analytics & Dashboard
  • Analytics dashboard global : sparklines, catégories, alertes
  • Skeleton de chargement structuré et animé sur le dashboard
  • Redesign formulaire ajout de site
  • Redesign tabs + scan reference header sur site-detail

  FEATURES — Notifications
  • Préférences de notifications : backend (champs users) + frontend (page profil)

  FEATURES — PDF
  • Refonte PDF audit : thème sombre moderne
  • Page de couverture unifiée sur tous les générateurs PDF
  • Fix : email contact dans les rapports → contact@rochercybersecurite.com
  • Fix qualité visuelle cover page + overflow bandes

  AUTH / UX
  • Fix : redirect vers landing après login (non vers dashboard)
  • Fix : utilisateurs authentifiés autorisés à visiter la landing

  TESTS
  • Suite de tests portée à 85% de couverture
  • Fix bug timezone datetime (UTC aware)


────────────────────────────────────────────────────────────────────────────────
  2026-04-14
────────────────────────────────────────────────────────────────────────────────

  ISO 27001
  • Onglet conformité ISO 27001:2022 complet (38 items, 10 domaines)
  • PDF cover page : gauge arc, KPI cards, sidebar violet
  • Redesign PDF : badges colorés, accents violets, barres de progression
  • Refactoring layout cover page (scores grille, fix overlaps)
  • Fix accent bar overflow sur cellules domaine

  UX
  • Redesign confirm dialog : thème sombre, icône, danger warning, boutons
  • Fix : régénération scripts de remédiation à la volée si manquants


────────────────────────────────────────────────────────────────────────────────
  2026-04-08
────────────────────────────────────────────────────────────────────────────────

  AUTH / UX
  • Redesign uniforme de toutes les pages d'authentification
  • Page login : panel gauche décoratif, inputs custom, stats animées


────────────────────────────────────────────────────────────────────────────────
  2026-04-05
────────────────────────────────────────────────────────────────────────────────

  FEATURES — Sécurité & Scan
  • Score de sécurité, jauge circulaire, radar chart et tendance
  • Findings UI : résultats complets persistés en base
  • FAQ redesign
  • Enforcement fréquence de scan (SMTP)

  FEATURES — Newsletter
  • Newsletter bi-hebdomadaire "Le Radar Cyber" (backend + frontend)
  • Section teaser sur la landing page


────────────────────────────────────────────────────────────────────────────────
  2026-04-04
────────────────────────────────────────────────────────────────────────────────

  FEATURES — Landing
  • Globe canvas animé en fond du hero (version 1 → 2 → 3)
    v3 : continent outlines, impacts, tilt, stats overlay
  • Section offres d'audit sur la landing

  AUTH
  • Forgot-password + reset-password (backend + frontend)
  • Migration Alembic : table password_reset_tokens

  SEO / LÉGAL
  • Pages CGU et Mentions légales + liens footer
  • Meta tags SEO sur pages Ressources et Bonnes pratiques

  FIXES
  • Material Icons via npm (au lieu de Google Fonts)
  • Fix police + navbar overflow


────────────────────────────────────────────────────────────────────────────────
  2026-04-03
────────────────────────────────────────────────────────────────────────────────

  FEATURES — Pages publiques
  • Page Ressources : liens externes + chaînes YouTube cybersécurité
  • Page Bonnes Pratiques : 4 sections de bonnes pratiques

  FEATURES — SaaS Rocher Cybersécurité (module principal)
  • Profil utilisateur, onboarding, succès Stripe
  • Pagination, export CSV, dialog de confirmation
  • Skeleton de chargement, thème dark, i18n
  • Tableau comparatif des plans tarifaires
  • Polling temps réel statut des scans
  • Badges sites, filtre scans, page détail scan
  • FAQ, témoignages clients, compteurs animés
  • Auth guard sur le dashboard
  • Page 404 personnalisée
  • Redirect vers register depuis dashboard non authentifié

  CYBER-SCANNER — Tier 4
  • Open redirect checker
  • Clickjacking detector
  • Directory listing checker
  • Robots.txt / sitemap analyzer
  • JWT checker

  FIXES
  • Auto-login après inscription (bypass authGuard onboarding)
  • Fix liens nav en blanc sur toutes les pages
  • Fix binding Angular template (class avec slash)


────────────────────────────────────────────────────────────────────────────────
  2026-04-02
────────────────────────────────────────────────────────────────────────────────

  CYBER-SCANNER — Tier 1, 2, 3
  • Tier 1 : email security, cookies, CORS, IP reputation
  • Tier 2 : DNS / sous-domaines, CMS detector, breach checker, WAF detector
    → 210 tests unitaires
  • Tier 3 : tech fingerprint, TLS audit, subdomain takeover, threat intel,
    HTTP methods checker
  • SCA (Software Composition Analysis), secrets detection, remédiation
    → 116 tests unitaires
  • Intégration modules Tier 1+2 dans main.py et rapport PDF

  DEPENDABOT
  • Merges : httpx 0.28.1, aiosqlite 0.22.1, python-dotenv 1.2.2,
    pytest 9.0.2, python-multipart 0.0.22


────────────────────────────────────────────────────────────────────────────────
  2026-04-01 — INITIALISATION DU PROJET
────────────────────────────────────────────────────────────────────────────────

  FONDATIONS
  • Initialisation complète du projet Cyber-Vault
  • Vault frontend (chiffrement côté client), vitest, corrections prod/alembic
  • Architecture complète : FastAPI + PostgreSQL + Angular + Docker
  • Robustesse : sécurité, logs, qualité, export
  • Documentation architecture.txt v4.0
  • Config Docker staging (backend + frontend + postgres)
  • Config Render staging
  • Premier pipeline GitHub Actions CI/CD
  • Outillage UX/UI : GSAP, AOS, animate.css, axe-core, Storybook, Compodoc


────────────────────────────────────────────────────────────────────────────────
  2026-05-20
────────────────────────────────────────────────────────────────────────────────

  REFACTORING
  • Suppression complète du module audit-pme (remplacé par les offres RSSI)
  • Fix landing : 5 tests périmés corrigés + lien Audit PME dans la navbar

  FEATURES — Scan & Dashboard
  • CTA "Audit Flash humain" dans les rapports de scan (avec pré-remplissage need_type)
  • Fix prix Audit Flash : 245 € → 290 € HT
  • Freemium teasing, comparateur Pro et courbe d'évolution du score de sécurité
  • Tuiles Quick Tools sur le dashboard

  TESTS
  • Specs scan-detail : getters CTA, criticalCount, warningCount, duration

  MODULE — Simulation Phishing (Phase 1 + 2)
  • Phase 1 : page vitrine /cyberscan/simulation-phishing
  • Phase 2 : remplacement GoPhish par moteur natif FastAPI
    - Générateur look-alike de domaines (typosquatting, homoglyphes, sous-domaines)
    - Wizard de création de campagne, liste, lancement, suivi de statut
    - Fix bug sim_subdomain coupé + gestion statut "sending"
  • Infrastructure tests in-memory SQLite : 52 tests phishing & auth
  • Tests unitaires : look-alike generator, statuts campagne


────────────────────────────────────────────────────────────────────────────────
  2026-05-22
────────────────────────────────────────────────────────────────────────────────

  MODULE — RSSI Externalisé (Sprint 1 + 2)
  • Sprint 1 — Fondations multi-tenant :
    - CRUD clients (formula essentiel/premium/excellence, CRM : Notion/Pipedrive/Pennylane)
    - CRUD visites (mensuelle, trimestrielle, annuelle, urgente — onsite/remote)
    - CRUD actions (priorité, statut, responsable, deadline, catégorie)
  • Sprint 2 — Dashboard agrégé multi-clients :
    - KPIs : MRR total, clients actifs, actions en retard, visites à venir
    - Alertes (actions en retard, renouvellements, absence de visite récente)
    - Événements calendrier (visites planifiées N jours à venir)
    - Suggestions rule-based (upsell, engagement, renouvellement, retard élevé)
  • Tests dashboard : 100 % de couverture sur toutes les branches alertes/suggestions

  FEATURES — Phishing
  • Personnalisation de l'expéditeur dans les campagnes
  • Fix flow de lancement (statut "sending" → "completed")
  • Config production : Resend + domaine vérifié

  REFACTORING / QUALITÉ
  • Backend : centralisation require_admin, safe_json_load, NEED_LABELS
  • Fix transaction scan (éviter double commit)
  • Frontend : fix memory leaks RxJS + retry 5xx dans l'interceptor HTTP
  • Boost couverture tests global : 40 % → 90 %
  • Fix mocks : get_active_plan → get_effective_max_sites, 401 → 403
  • Fix : pin python-multipart 0.0.27 (CVE GHSA-pp6c-gr5w-3c5g)


────────────────────────────────────────────────────────────────────────────────
  2026-05-23
────────────────────────────────────────────────────────────────────────────────

  MODULE — Simulation Phishing (finalisation)
  • Pages détail campagne et édition de campagne
  • Tests unitaires : service, campaign-detail, campaign-edit
  • Suite Playwright E2E complète (phishing)

  TESTS — E2E
  • Suite Playwright globale : 48 tests, 0 failed

  MODULE — RSSI Externalisé (Sprint 5 + P8 + Sécurité)
  • Sprint 5 :
    - CRUD livrables (compte-rendu, rapport, recommandation, contrat, autre)
    - Liaison/déliaison de sites à un client RSSI
    - Rapport PDF (ReportLab) par client
    - Page profil consultant (display_name, company_name, phone)
    - Export CSV des actions (UTF-8 BOM)
  • P8 : upload fichier pour les livrables (S3 + fallback local dev)
  • Sécurisation complète du module (2026-05-23 soir) :
    - is_rssi_consultant ajouté dans UserOut (exposé via /users/me)
    - Tous les endpoints /rssi/* utilisent get_rssi_consultant (→ 403 si non consultant)
    - PATCH /admin/users/{id}/rssi : toggle du rôle consultant depuis l'admin
    - rssiGuard Angular : vérifie is_rssi_consultant avant accès aux routes
    - Admin panel : colonne RSSI + bouton toggle
  • Tests frontend : rssi.service.spec.ts, consultant-dashboard.spec.ts, client-detail.spec.ts
  • Liens navigation : menu utilisateur + footer sur la landing

  MODULE — Dark Web Dossier B2B (complet)
  • Upload CSV d'emails → analyse HIBP + LeakCheck en background
  • Risk scoring (0–100) + severity scoring pondéré par type de données exposées
    (passwords=4, financial=5, phone/address=2, email/name=1)
  • Rescan on-demand, export CSV (UTF-8 BOM), toggle monitoring mensuel
  • Alerte email automatique si nouvelles fuites détectées (APScheduler cron 03h00)
  • Timeline chart des fuites par année (bar chart normalisé)
  • Rapport PDF (ReportLab)
  • Fix : time.sleep → asyncio.sleep + asyncio.to_thread (respect rate limits LeakCheck)
  • Fix : session DB isolée dans process_dossier, garde admin, limite par utilisateur
  • Cartes "Surveillance Dark Web" et "Dark Web Dossier" dans les Quick Tools du dashboard
  • Tests : intégration (rescan, CSV, monitor), unit (_compute_severity, export_dossier_csv),
    frontend service + composant (severity, timeline, getBreachTimelineMax)

  AMÉLIORATIONS — Module Dark Web Dossier (intégrité + UX + qualité)
  • P0 — Correction faux négatifs silencieux :
    - Nouveau champ check_status (verified_clean | exposed | api_error | rate_limited | pending)
      sur darkweb_dossier_targets
    - Les emails non vérifiés (rate limit / erreur API) reçoivent status="error" au lieu de
      status="clean" — plus de résultats mensongers
    - Le score de risque est désormais calculé sur les emails vérifiés uniquement
  • P1 — Suivi de progression en temps réel :
    - Nouveau champ checked_count + unverified_count sur darkweb_dossiers
    - Commit après chaque email → polling frontend affiche "X / Y emails vérifiés"
    - Barre de progression animée dans le banner "Analyse en cours"
    - Banner d'avertissement orange affiché si unverified_count > 0 après analyse
    - Section "Emails non vérifiés" avec badge rate_limited / api_error par adresse
    - Migration Alembic : a3b4c5d6e7f8_darkweb_check_status_progress.py
  • P2 — Enrichissement et qualité :
    - _BATCH_DELAY : 1.2 → 2.5 s (meilleure tolérance LeakCheck free tier)
    - Recommandations PDF dynamiques via _build_recommendations() basées sur :
      les data_classes trouvées (MFA si password, alerte financière si credit/bank,
      RGPD si données sensibles, avertissement si analyse incomplète)
    - CSV export inclut désormais la colonne "Vérification API" (check_status)
    - Rescan reset check_status, checked_count et unverified_count


================================================================================
  FIN DU JOURNAL — Mis à jour le 2026-05-23
================================================================================
